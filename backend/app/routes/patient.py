# Endpoints for patient data management (add, delete, query notes)
"""
POST /patients/{id}/notes: Add a new note.
DELETE /patients/{id}/notes/{note_id}: Delete a note.
GET /patients/{id}/history: Query patient history.
"""